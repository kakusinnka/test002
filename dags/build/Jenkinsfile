pipeline {
    agent any

    stages {
        stage('modify dags') {
            steps {
                dir ('dags') {
                    sh('./build/update.sh')
                }
            }

        }
    }
}
