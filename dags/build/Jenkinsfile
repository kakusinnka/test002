pipeline {
    agent any

    stages {
        stage('modify dags') {
            steps {
                dir ('dags') {
                    sh('chmod u+x ./build/update.sh')
                    sh('./build/update.sh params.start_date')
                }
            }

        }
    }
}
