pipeline {
    agent any

    stages {
        stage('modify dags') {
            steps {
                dir ('dags') {
                    sh('sudo ./build/update.sh')
                }
            }

        }
    }
}
